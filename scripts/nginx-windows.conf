server {
    listen 80;
    server_name _;  # 接受所有域名，或改为你的域名

    # 日志配置
    access_log C:/nginx/logs/miniapp-api-access.log;
    error_log C:/nginx/logs/miniapp-api-error.log;

    # 请求体大小限制
    client_max_body_size 50M;

    # API 代理
    location / {
        proxy_pass http://localhost:9080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态文件缓存（如果有）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        proxy_pass http://localhost:9080;
        proxy_cache_valid 200 1d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS 配置（获取 SSL 证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     # SSL 证书配置
#     ssl_certificate C:/nginx/ssl/your-domain.crt;
#     ssl_certificate_key C:/nginx/ssl/your-domain.key;
#
#     # SSL 优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     # 日志配置
#     access_log C:/nginx/logs/miniapp-api-ssl-access.log;
#     error_log C:/nginx/logs/miniapp-api-ssl-error.log;
#
#     # 请求体大小限制
#     client_max_body_size 50M;
#
#     # API 代理
#     location / {
#         proxy_pass http://localhost:9080;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
#
# # HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
